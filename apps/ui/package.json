{
  "name": "fidorust",
  "private": true,
  "version": "0.1.7",
  "type": "module",
  "scripts": {
    "wasm": "wasm-pack build ../../crates/fidocad-wasm --target web --out-dir ../../apps/ui/src/wasm --out-name fidocad_wasm",
    "dev": "npm run wasm && vite",
    "build": "npm run wasm && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "cd ../../apps/ui && tauri dev"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.1.0",
    "@tauri-apps/cli": "^2.8.0",
    "svelte": "^5.38.0",
    "typescript": "^5.9.0",
    "vite": "^6.3.0",
    "wasm-pack": "^0.15.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "@tauri-apps/plugin-dialog": "^2.4.0",
    "@tauri-apps/plugin-fs": "^2.4.0",
    "@tauri-apps/plugin-opener": "^2.4.0"
  }
}
